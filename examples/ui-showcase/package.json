{
  "name": "ui-showcase",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "VICT UI Showcase / Owner Visual Acceptance App: one neutral vict.application@2 definition (P6D scenario showroom + stress laboratory) rendered by the permanent Svelte ApplicationRenderer. The visible application is authored through the normal VICT path (Application Definition -> @victframework/application -> @victframework/ui -> @victframework/ui-svelte); the host adds no product styling of its own.",
  "scripts": {
    "prepare": "svelte-kit sync || echo ''",
    "dev": "vite dev",
    "build": "svelte-kit sync && vite build",
    "preview": "vite preview",
    "check": "svelte-kit sync && tsc -p tsconfig.json --noEmit",
    "capture": "node scripts/capture.mjs",
    "test": "vitest run"
  },
  "dependencies": {
    "@victframework/application": "0.3.1",
    "@victframework/sdk": "0.3.1",
    "@victframework/runtime": "0.3.1",
    "@victframework/ui": "0.3.1",
    "@victframework/ui-svelte": "0.3.1",
    "@victframework/renderer-svelte": "0.3.1"
  },
  "devDependencies": {
    "@sveltejs/adapter-node": "^5.2.12",
    "@sveltejs/kit": "^2.20.0",
    "@sveltejs/vite-plugin-svelte": "^5.0.3",
    "axe-core": "^4.10.0",
    "happy-dom": "^15.11.7",
    "puppeteer-core": "^24.0.0",
    "svelte": "^5.28.0",
    "vite": "^6.3.5"
  },
  "license": "Apache-2.0"
}
