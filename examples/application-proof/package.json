{
  "name": "application-proof",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Stage 04 SvelteKit vertical proof: one neutral Application Definition rendered through a generic catch-all host, with a typed resource view, a contract-validated form, a local action, a real VICT capability action, and a custom component resolved by id/revision.",
  "scripts": {
    "prepare": "svelte-kit sync || echo ''",
    "build": "svelte-kit sync && vite build",
    "preview": "vite preview",
    "test": "vitest run"
  },
  "devDependencies": {
    "@sveltejs/kit": "^2.20.0",
    "@sveltejs/vite-plugin-svelte": "^5.0.3",
    "happy-dom": "^15.11.7",
    "svelte": "^5.28.0",
    "vite": "^6.3.5",
    "@sveltejs/adapter-node": "^5.2.12"
  },
  "dependencies": {
    "@vict/application": "0.1.0",
    "@vict/runtime": "0.1.0",
    "@vict/sdk": "0.1.0"
  }
}
