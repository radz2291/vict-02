{
  "name": "vict-p5qa-harness",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "QA-only browser harness for P4 verification (not part of the release set; lives outside the workspace globs).",
  "scripts": {
    "plan": "tsx scripts/emit-plan.mts",
    "dev": "vite dev --port 5201 --strictPort",
    "build": "vite build",
    "preview": "vite preview --port 5201 --strictPort"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^5.0.3",
    "axe-core": "^4.10.0",
    "svelte": "^5.28.0",
    "tsx": "^4.19.3",
    "vite": "^6.3.5"
  }
}
