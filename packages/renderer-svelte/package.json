{
  "name": "@vict/renderer-svelte",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Canonical SvelteKit renderer for Vict Application Plans: generic application host, built-in role components, responsive navigation, semantic theme tokens, and accessible defaults. Svelte and component-library dependencies live ONLY in this renderer boundary — never in @vict/application or the base SDK.",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "svelte": "./src/index.ts",
      "default": "./src/index.ts"
    },
    "./theme.css": "./src/theme.css"
  },
  "main": "./src/index.ts",
  "svelte": "./src/index.ts",
  "types": "./src/index.ts",
  "files": [
    "src",
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json --emitDeclarationOnly --outDir dist",
    "test": "vitest run --project renderer"
  },
  "dependencies": {
    "@vict/application": "0.1.0",
    "@vict/sdk": "0.1.0"
  },
  "peerDependencies": {
    "svelte": "^5.0.0"
  },
  "engines": {
    "node": ">=22.13.0"
  }
}
